# 协作手册 · SF TECH WEEK CITY

给这个仓库的开发者。英文的完整说明在 [README.md](README.md)，这份是上手和日常操作。

---

## ⚠️ 先记住一件事

**你 push 到 `main`，30 秒后线上游戏就变了，没有任何人审核。**

这不是比喻。实测过一次：12:54:25 push，12:54:55 线上版本号从 rev 6 跳到 rev 7。GitHub Actions 会自动构建、提交、批准、合并、发布，全程无人参与。

所以：

- 在**分支**上开发，随便折腾，不会影响线上。
- 合进 `main` 之前，先在本地跑一遍确认没问题。
- 你有 `admin` 权限，`main` 没有分支保护——技术上你可以直接 `git push origin main`。**能做不等于该做**，重要改动建议走 PR 让人看一眼。

---

## 1. 环境

| 需要 | 说明 |
|---|---|
| Node.js 20+ | 实测 24.14.1 |
| Git | — |
| 任意静态服务器 | 例子用 `python3 -m http.server`，用什么都行 |

**没有 npm install。** 这个项目零依赖，构建脚本只用 Node 内置模块。看到 `package.json` 别习惯性 `npm i`，没用。

## 2. 跑起来

```bash
git clone git@github.com:Alterverse-tech/city-in-ink.git && cd city-in-ink
npm run build:local
python3 -m http.server 8790 --directory dist-local
```

打开 http://localhost:8790 。构建约 1 秒。

**不要用 `npm run build` 的产物 `dist/` 在本地打开。** 那是 Chrona 托管版，脱离 Chrona 的 iframe 会直接白屏报 `A trusted HTTPS host origin is required`——这是设计如此，不是 bug，别去"修"它。本地一律用 `build:local` → `dist-local/`。

没有热更新，改完代码要重新 `npm run build:local` 再刷新页面。

## 3. 改哪里

**两个编辑面，规则不一样。**

**A. 附加模块**——直接改，重新构建即可：

```
multiplayer.js / multiplayer.css    多人 UI
events-sync.js / events-sync.css    活动数据同步、海报加载
gull-cluster-route.mjs              海鸥飞行路线
network-pose.js                     联机位置编码
chrona/                             Chrona SDK
build.mjs                           构建期对原始游戏打的补丁（含改名 wireBrand）
```

**B. 原始城市** `source/part-*.txt`——这是一个 24 MB 的单文件 HTML 被切成 49 片（Chrona 限制单个文本文件 ≤ 4 MiB）。改完必须刷新哈希再提交：

```bash
node build-hosted.mjs --accept-source-update
git add source/ && git commit
```

不加 `--accept-source-update` 构建会直接拒绝运行——这是防止切片文件被意外损坏的保护，**不要绕过它**。

构建脚本用**精确字符串匹配**给原始游戏打补丁。如果你改了 source 导致某个补丁目标消失，构建会报 `patch target changed: ...`。这时候要么改补丁要么改 source，**不要注释掉检查**。

## 4. 日常流程

**方式一：走 PR（推荐，重要改动用这个）**

```bash
git checkout -b feat/你的功能
# 改代码，npm run build:local 本地验证
git commit -am "说明"
git push -u origin feat/你的功能
gh pr create --base main
```

合并进 main 后自动上线。**合并不会让你的分支失效**，你可以继续在同一个分支上开发，以后再合一次。

**方式二：直接推 main（小改动、你确信没问题时）**

```bash
git commit -am "说明" && git push origin main
```

30 秒后线上生效。

**只有 `.md` 文件或 `.github/` 的改动不会触发发布**，改文档可以放心推。

## 5. 想给人看效果，但不想上线

不要为了演示去推 main。本地生成一个 Chrona 预览链接：

```bash
NODE_USE_ENV_PROXY=1 \
CHRONA_CLI=/path/to/chrona.mjs \
CHRONA_WORLD='https://chrona.world/studio/?world=e9ef2f62-a6e0-47f2-8795-c2941cbc433a' \
CHRONA_SUBMIT_STOP_AT=preview \
node .github/chrona-release.mjs
```

会打印一个 Chrona 预览链接，可以直接发给人看，**线上不受影响**。（`CHRONA_CLI` 是 `chrona-game` 插件里的 `scripts/chrona.mjs`；`NODE_USE_ENV_PROXY=1` 只在你挂代理时需要——Node 默认不认 `HTTPS_PROXY`，不加会卡住 120 秒然后超时。）

**绝对不要**在非 `main` 分支上跑 `gh workflow run chrona-release.yml --ref 你的分支`。那会把你分支的代码直接发到线上，导致 GitHub main 和线上分叉——之后任何人推一次 main，你的改动就被静默回滚了。

## 6. 这些"报错"是正常的，不用修

本地跑的时候控制台会有一堆红色，以下都是预期的：

- `cdn.tech-week.com` 图片 CORS 报错 —— 活动海报跨域，游戏会用自带海报兜底。
- `/events.json` 404 —— 静态服务器没有日历服务，游戏用 `data/` 里的存档数据。
- `/assets/airship.glb`、`/assets/brand/techweek-logo.svg` 404 —— 可选资源，有内置兜底，**线上也是 404**。
- 登录和联机连的是**测试**环境（`livemode: false`）。

## 7. 不要提交的东西

```
.chrona/        Chrona 工作区绑定（含凭据关联）
dist/           托管构建产物
dist-local/     本地构建产物
dist-artifact/  单文件构建产物
```

`.gitignore` 已经覆盖，但别手动 `git add -f`。

## 8. 出问题了

- **线上发布挂了**：看 [Actions](https://github.com/Alterverse-tech/city-in-ink/actions)。如果日志显示 merge 成功但 publish 失败，run 摘要会明确写"Chrona main 已推进但线上未更新"并给出手动发布链接。
- **要回滚线上**：找 ppeng。Chrona 保留完整版本历史，`chrona.mjs restore --commit <sha>` 几分钟能恢复到任意历史版本。
- **CI 报凭据过期**：发布凭据 **2026-12-08 到期**（Chrona 的 World 级发布凭据必然有有效期）。CI 会提前 14 天警告。重新签发要 ppeng 操作，命令在 README 的 "Automatic release from GitHub (CI)" 一节。

## 9. 用 Claude Code 的话

仓库根目录有 **`CLAUDE.md`**，Claude Code 开会话时会自动读，里面写了上面这些约束（别 npm install、别开 dist/、推 main = 上线等），所以你直接说需求就行。

**如果 Claude 报 `not in this session's authorized repository set` 或"只有匿名读取权限"**：那是云端会话（claude.ai/code）没绑定你的 GitHub 账号。两个解法——

1. 在**本机终端**跑 Claude Code，用你自己的 SSH key / `gh auth login`；
2. 或者去 claude.ai → Settings → Claude Code 连接 GitHub，授权账号选 `brucehuang1`，然后**新开一个会话**（旧会话的授权范围是创建时定死的，不会扩）。

---

## 关键信息速查

| | |
|---|---|
| 仓库 | https://github.com/Alterverse-tech/city-in-ink （私有） |
| 线上游戏 | https://chrona.world/play/e9ef2f62-a6e0-47f2-8795-c2941cbc433a/ （需登录） |
| CI | https://github.com/Alterverse-tech/city-in-ink/actions |
| 你的权限 | `admin`，可直接推 `main` |
| 上线延迟 | push 后约 30 秒 |
